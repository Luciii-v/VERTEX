# Pin and pre-download BEFORE going air-gapped:
#   pip download -r requirements.txt -d wheels/
#   pip install --no-index --find-links wheels/ -r requirements.txt
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pymupdf==1.25.1          # PDF text + page rendering
python-docx==1.1.2       # approval notes
openpyxl==3.1.5          # spreadsheets
python-pptx==1.0.2       # management decks
pytesseract==0.3.13      # OCR fallback (needs the tesseract binary)
pillow==11.1.0
# paddleocr + paddlepaddle: heavier, install only on the M5 if you want it
